#pragma once
#ifndef UTILS_H
#define UTILS_H

#include <iostream>
#include <string>

std::string insert_word(std::string target, std::string input, int index);
int word_count(std::string target);

#endif // !UTILS_H
